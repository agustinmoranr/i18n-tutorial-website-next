import { LOCALES_LIST } from './types';

export const DEFAULT_LOCALE = LOCALES_LIST.ES;
export const HOME_ROUTE = '/';

export const LOCALE_TEXT = {
	[LOCALES_LIST.ES]: 'ES',
	[LOCALES_LIST.EN]: 'EN',
};
